# Copy to .env and fill in your values.
# .env is in .gitignore — secrets never go to version control.

# LLM provider API keys (required only for cloud providers, not for Ollama)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# Logging format: "json" for production/web, omit for human-readable CLI output
# LOG_FORMAT=json
